{
  "name": "@vazue/queue-edge-cloudfront",
  "version": "0.1.0",
  "description": "CloudFront Lambda@Edge connector for Vazue Queue",
  "license": "Apache-2.0",
  "main": "dist/handler.js",
  "types": "dist/handler.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "prepublishOnly": "pnpm build"
  },
  "engines": {
    "node": ">=20"
  },
  "private": true,
  "devDependencies": {
    "typescript": "^5.9.2",
    "@types/node": "^24.2.0",
    "vitest": "^3.2.4",
    "@types/aws-lambda": "^8.10.152"
  }
}
