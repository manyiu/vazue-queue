---
"@yiu/queue-cdk": patch
---

Ship updated queue Lambda binaries: jsonwebtoken 10 (`aws_lc_rs`) and Turnstile secret loading from Secrets Manager on EnrollFn (scoped env/IAM, challenge-mode verification fixes).
