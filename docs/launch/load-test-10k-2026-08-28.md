# Load test 10K record — 2026-08-28

In-region `PROFILE=rc` run at **10,000** concurrent status pollers (single CodeBuild worker).

## Verdict

| Gate | Threshold | Observed | Pass? |
|------|-----------|----------|-------|
| HTTP fail rate | &lt; 1% | **0.03%** | Yes |
| `http_req_duration` p95 | &lt; 250ms | **38.6ms** | Yes |
| `http_req_duration` p99 | &lt; 500ms | *(not in summary JSON)* | — (k6 exit 0) |

**Overall:** **PASS** (RC thresholds on fail rate + p95)

## Interpretation

- **10K concurrent pollers** passes RC SLOs on the default `minimal` stack (512 MB Lambda, enroll buffer off).
- The earlier **100K** miss (~71% failures) was not a single-worker Lambda concurrency cap at 10K — likely **aggregate API Gateway / Lambda throttling** when many workers hammer the API together.
- Note: AWS account **Lambda concurrent execution** default is **1,000** per region, not 10K. Status polling with adaptive `poll_after_seconds` keeps in-flight Lambda count well below concurrent poller count — see [`docs/deploy/capacity.md`](../deploy/capacity.md).

## Conditions

| Item | Value |
|------|--------|
| Date | 2026-08-28 ~22:38–22:51 HKT (us-east-1) |
| AWS account | `517206156255` |
| Region | **us-east-1** |
| Stack | `VazueQueueLoadTestRc` (destroyed after run) |
| Preset | **`minimal`** |
| Enroll buffer | **off** |
| Lambda memory | **512 MB** |
| `QUEUE_API_URL` | `https://pwpd7qblh8.execute-api.us-east-1.amazonaws.com` |
| `EVENT_ID` | `loadtest-10k` |
| Load generator | k6 v0.54.0, CodeBuild `BUILD_GENERAL1_2XLARGE`, **1 worker** |
| Command | `SKIP_BUILD=1 VUS=10000 WORKERS=1 EVENT_ID=loadtest-10k bash scripts/run-load-test-100k-inregion.sh` |
| Iterations | **1,546,186** |

## Raw metrics

[`load-test-10k-2026-08-28.json`](./load-test-10k-2026-08-28.json)
