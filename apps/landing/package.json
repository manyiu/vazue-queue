{
  "name": "@vazue/landing",
  "version": "0.1.0",
  "private": true,
  "description": "Static marketing page for queue.vazue.com",
  "scripts": {
    "dev": "python3 -m http.server 4173 --directory .",
    "build": "mkdir -p dist && cp index.html styles.css dist/"
  }
}
